# POD Automation Engine
NEO POD (Alsaduquon) generates designs, validates listings, prices for margin, and publishes to Etsy automatically. Pipeline: generate -> validate -> mockup -> list -> optimize. Runs on a cron or by voice command. Costs API money per run, so runs are deliberate. Feeds [[Revenue Tracking]] and [[Etsy Listing SEO]].
